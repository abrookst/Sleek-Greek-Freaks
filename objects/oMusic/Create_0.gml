
// Room is a global variable that gets the current room
switch(room){
	case Room1: audio_stop_all(); audio_play_sound(mBattle,500,true);
	case test: audio_stop_all(); audio_play_sound(mVibes,500,true);
}

